pkg_name=systemd
version=261.2
revision=1
build_style=meson
short_desc="System and service manager"
home_page="https://systemd.io"
license="LGPL-2.1-or-later"
dist_files="https://github.com/systemd/systemd/archive/refs/tags/v${version}.tar.gz>systemd-${version}.tar.gz"
checksum="7352f4c878324176fcefae233d4f9aeab6319b48b594bb23a0ddc38289ec26c6"
wrk_src="systemd-$version"
depends="acl attr bzip2 kmod libelf libfido2 libgcrypt gnutls libidn2 linux-pam libmicrohttpd libqrencode libseccomp libucontext lz4 openssl pcre2 tpm2-tss util-linux xz zlib zstd"
make_depends="acl attr bzip2 kmod libelf libfido2 libgcrypt gnutls libidn2 linux-headers linux-pam libmicrohttpd libqrencode libseccomp libucontext lz4 openssl pcre2 tpm2-tss util-linux xz zlib zstd"
host_make_depends="bison docbook-xsl flex gperf libxslt meson pkgconf python python-jinja2 samurai"

use_flags="
    seccomp pam libcrypt libcryptsetup kmod lz4 elfutils coredump idn
    gcrypt gnutls microhttpd qrencode libfido2 tpm2 bpf-framework efi
    bootloader man doc apparmor polkit smack ima ipe utmp userdb
    networkd resolve timesyncd logind hostnamed localed timedated
    initrd firstboot sysusers tmpfiles hwdb randomseed backlight
    vconsole rfkill repart sysupdate kernel-install
"
use_default="
    +seccomp +pam +libcrypt +libcryptsetup +kmod +lz4 +elfutils +coredump
    +idn +gcrypt +gnutls +microhttpd +qrencode +libfido2 +tpm2 -man -doc
    +smack +ima +ipe +utmp +userdb +networkd +resolve +timesyncd +logind
    +hostnamed +localed +timedated +initrd +firstboot +sysusers +tmpfiles
    +hwdb +randomseed +backlight +vconsole +rfkill +repart +sysupdate
    +kernel-install +bpf-framework -efi -bootloader -apparmor -polkit
"

configure_args="
    -Dlibc=musl
    -Dacl=enabled
    -Dblkid=enabled
    -Dfdisk=enabled
    -Dlibmount=enabled
    -Dopenssl=enabled
    -Dpcre2=enabled
    -Dzlib=enabled
    -Dbzip2=enabled
    -Dxz=enabled
    -Dzstd=enabled
    -Dxkbcommon=enabled
    -Dglib=disabled
    -Ddbus=disabled
    -Dp11kit=disabled
    -Dtranslations=disabled
    -Dquotacheck=disabled
    -Dtests=false
    $(use_bool seccomp)
    $(use_bool pam)
    $(use_bool libcrypt)
    $(use_bool libcryptsetup)
    $(use_bool kmod)
    $(use_bool lz4)
    $(use_bool elfutils)
    $(use_bool coredump)
    $(use_bool idn)
    $(use_bool gcrypt)
    $(use_bool gnutls)
    $(use_bool microhttpd)
    $(use_bool qrencode)
    $(use_bool libfido2)
    $(use_bool tpm2)
    $(use_bool tpm2 tpm)
    $(use_bool bpf-framework)
    $(use_bool efi)
    $(use_bool bootloader)
    $(use_bool man)
    $(use_bool doc html)
    $(use_bool apparmor)
    $(use_bool polkit)
    $(use_bool smack)
    $(use_bool ima)
    $(use_bool ipe)
    $(use_bool utmp)
    $(use_bool userdb)
    $(use_bool networkd)
    $(use_bool resolve)
    $(use_bool timesyncd)
    $(use_bool logind)
    $(use_bool hostnamed)
    $(use_bool localed)
    $(use_bool timedated)
    $(use_bool initrd)
    $(use_bool firstboot)
    $(use_bool sysusers)
    $(use_bool tmpfiles)
    $(use_bool hwdb)
    $(use_bool randomseed)
    $(use_bool backlight)
    $(use_bool vconsole)
    $(use_bool rfkill)
    $(use_bool repart)
    $(use_bool sysupdate)
    $(use_bool kernel-install)
"
